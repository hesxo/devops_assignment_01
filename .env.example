PORT=3000

DB_HOST=asthra-dev-master-server.cluster.asthrahealthtech.com
DB_PORT=30432
DB_NAME=devops_sandbox
DB_USER=devops_sandbox
# Quote passwords that contain # (otherwise the rest is treated as a comment)
DB_PASSWORD=your_password_here
DB_SSL=true

# For docker compose, FILE_PATH is set in docker-compose.yaml.
# For local npm run: FILE_PATH=./data/data.txt
