apiVersion: v1
kind: Service
metadata:
  name: devops-node-api-service
  namespace: devops-sandbox
spec:
  type: ClusterIP
  selector:
    app: devops-node-api
  ports:
    - port: 80
      targetPort: 3000
